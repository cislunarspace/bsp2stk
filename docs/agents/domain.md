# Domain docs

## Layout

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root.

## Consumer rules

When a skill asks for domain context, read `CONTEXT.md` at the repo root.
When a skill asks for architectural decisions, read `docs/adr/*.md`.
